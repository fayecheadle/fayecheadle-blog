---
title: Spread Good Ideas
---

<ul>
  <% blog.articles.each do |article| %>
    <li>
      <h3><%= link_to article.title, article.path %>, <%= article.date.strftime('%b %e') %></h3>
      <p><%= article.body %></p>
    </li>
  <% end %>
</ul>
